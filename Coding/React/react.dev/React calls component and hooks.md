_Components should only be used in JSX_. Don’t call them as regular functions. React should call it.

_Hooks should only be called inside of components or hooks_. Never pass it around as a regular value.