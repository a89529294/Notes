Every string consists of a *sequence of bytes*. The bytes map to characters. Exactly how they map to characters—how many bytes make up a character, and what those characters are—is a matter of *encoding*.

